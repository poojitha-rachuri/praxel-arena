// TODO: GET /api/profile - Own profile with skill scores + Elo + career match %
import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ error: "Not implemented" }, { status: 501 });
}
